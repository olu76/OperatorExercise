using System;

namespace OperatorExerciseConsole
{
    class Program
    {
        static void Main(string[] args)
        {
            double a = 17;
            double b = 4;
            double sum;
            sum = a + b;
            double minus;
            minus = a - b;
            double multiply;
            multiply = a * b;
            double divide;
            divide = a / b;

            double qoutient;
            qoutient = a / b;
            double remainder;
            remainder = a % b;

            Console.WriteLine($" {sum}. {minus}. {multiply}. {divide}. {remainder}.");



            
            

        }
    }
}
